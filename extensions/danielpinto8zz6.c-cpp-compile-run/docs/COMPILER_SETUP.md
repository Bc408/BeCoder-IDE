# Compiler Setup

BeCoder Runner uses the GCC toolchain bundled with BeCoder. No system compiler setup is required.

The native PowerShell terminal remains separate and does not influence the compiler used by Runner.
